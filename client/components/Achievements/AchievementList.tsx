import React from "react";
import { Achievement } from ".";

export const AchievementList = ({
  appid,
  steamUserId,
}: {
  appid: string;
  steamUserId?: string;
}) => {
  return <Achievement />;
};
